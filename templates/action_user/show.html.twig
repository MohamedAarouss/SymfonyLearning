{% extends 'base.html.twig' %}

{% block title %}ActionUser{% endblock %}

{% block body %}
    <h1>ActionUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ action_user.id }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ action_user.createdAt ? action_user.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Action</th>
                <td>{{ action_user.action }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('action_user_index') }}">back to list</a>

    <a href="{{ path('action_user_edit', {'id': action_user.id}) }}">edit</a>

    {{ include('action_user/_delete_form.html.twig') }}
{% endblock %}
