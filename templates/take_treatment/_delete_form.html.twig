<form method="post" action="{{ path('take_treatment_delete', {'id': take_treatment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ take_treatment.id) }}">
    <button class="btn">Delete</button>
</form>
